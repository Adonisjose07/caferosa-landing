services:
  app:
    build: .
    ports:
      - "3005:3005"
    env_file:
      - .env
    restart: unless-stopped
    # Para desarrollo, puedes montar el código en caliente:
    # volumes:
    #   - ./:/app
