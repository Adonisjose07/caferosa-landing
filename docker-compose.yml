services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    # Para desarrollo, puedes montar el código en caliente:
    # volumes:
    #   - ./:/app
